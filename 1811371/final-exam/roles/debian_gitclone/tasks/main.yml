---
- name: Install git on debian
  apt:
   name: git
   state: latest

- name: Clone Prelim Exam branch on debian
  git:
    repo: '{{ prelim_exam }}'
    dest: ./prelim-exam
    version: prelim-exam
# tasks file for roles/debian_gitclone